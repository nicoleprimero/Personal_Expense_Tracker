import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.personal_expense_tracker',
  appName: 'Personal_Expense_Tracker',
  webDir: 'dist'
};

export default config;
