import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Personal_Expense_Tracker',
  webDir: 'dist'
};

export default config;
